fn myfunc(u8 arg1, u8 arg2, u32 ... args) {
    return arg1 + arg2;
};

myfunc(arg1, arg2, arg3);

while(a < 50 && check()) {
    if (x == 36.5)
        break;
    else {
        a += 5;
        continue;
    }
}
